Query: LET _ <= import(artifact="Server.Import.ArtifactBundle")
Output: []

Query: SELECT ("Exchange",) + Tags(Data=Def), ("Exchange",) + Tags(Data='') FROM scope()
Output: [
 {
  "(\"Exchange\", ) + Tags(Data=Def)": [
   "Exchange",
   "hello",
   "world"
  ],
  "(\"Exchange\", ) + Tags(Data='')": [
   "Exchange"
  ]
 }
]

