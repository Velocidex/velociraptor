Query: SELECT * FROM profile(metrics=TRUE) WHERE name =~ "client_comms_current_connections"
Output: [
 {
  "name": "client_comms_current_connections",
  "help": "Number of currently connected clients.",
  "value": 0,
  "Profile": "Metrics"
 }
]

