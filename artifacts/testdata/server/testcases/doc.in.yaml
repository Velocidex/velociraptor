Queries:
  - SELECT basename(path=OfficePath) as OfficePath,
        OfficeSize, InternalMtime, HexContext
    FROM Artifact.Generic.Applications.Office.Keywords(
        searchGlob=srcDir + '/artifacts/testdata/files')
