Queries:

  # Test Sinkhole remediation - output should be only default artifact entry
  - SELECT hash(filename=FullPath) FROM stat(filename='C:\\Windows\\System32\\drivers\\etc\\hosts')
  - SELECT *, hash(filename=HostsFileBackup) FROM Artifact.Windows.Remediation.Sinkhole()

  # Test rolling back sinkhole - output none
  - SELECT * FROM Artifact.Windows.Remediation.Sinkhole(RestoreBackup="True")
  - SELECT hash(filename=FullPath) FROM stat(filename='C:\\Windows\\System32\\drivers\\etc\\hosts')
