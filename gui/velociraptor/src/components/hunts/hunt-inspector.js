import React from 'react';
import PropTypes from 'prop-types';

export default class HuntInspector extends React.Component {
    static propTypes = {
        hunt: PropTypes.object,
    };

    render() {
        return (
            <>

            </>
        );
    }
};
